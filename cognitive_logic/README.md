# Cognitive Logic Area

## Purpose and ownership

This active area is reserved for provenance-labelled inherited comparison baselines and Obid's later cognitive contribution. Yacoub-originated baselines remain `YACOUB_INHERITED`; new Obid agent artifacts are `OBID_CREATED`.

## Current and planned population

- Step 6: the verified inherited deterministic anchor and minimal-agent `CONFIG-BASELINE` are under `baselines/yacoub/`, with sanitized exports, exact prompt/no-memory records, provenance, and bounded readiness evidence.
- Step 7: `obid/` now contains CONFIG-OBID's single-agent workflow, system prompt, exactly two tools, controlled ReAct-style behavior, internal structured decision, one bounded-memory configuration, and complete bounded readiness evidence. Fresh-session execution 33 completed the required threshold-then-status trace and internal no-op; historical quota failures remain retained in the Step 7 evidence record.
- Step 8 adds `obid/workflows/obid-agent-v2-safety.json`, a new CONFIG-OBID runtime version that preserves the frozen Step 7 cognition and places the reusable parser/schema validator and deterministic policy before action endpoints. The Step 7 v1 snapshot remains unchanged.
- Step 9 adds inactive, sanitized `obid/workflows/obid-agent-v3-hitl.json`. It preserves the Step 7 cognition and Step 8 enforcement while adding the actual caller-embedded Wait/form approval and denial path required by the pinned runtime.

## Boundary

The Step 6 files reproduce collaborator-provided baselines and do not transfer authorship. Do not copy the whole Yacoub `cognitive_logic/` tree, add Obid features to inherited baselines, rewrite frozen Step 7/8 history, describe pending evidence as verified, or begin Step 10 evaluation here.
